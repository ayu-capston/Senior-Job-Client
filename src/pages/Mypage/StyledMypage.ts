import styled from 'styled-components';

const body = styled.div`
    display: flex;
    flex-direction: row;
    margin: 20px 140px;
`;

const content = styled.div`
    gap: 14px 0px;
    width: 100%;
    margin: 50px 0px;
`;

export { body, content };
