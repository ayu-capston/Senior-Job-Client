import styled from 'styled-components';

const DatePickerSection = styled.section`
    display: flex;

    align-items: center;
    gap: 0px 10px;
    font-size: 1.6rem;
`;

export { DatePickerSection };
