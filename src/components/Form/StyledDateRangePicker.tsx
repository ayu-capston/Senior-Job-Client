import styled from 'styled-components';

const DatePickerSection = styled.section`
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 0px 10px;
`;

export { DatePickerSection };
